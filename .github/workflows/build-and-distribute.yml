name: Build GenZSpace

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build-android:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'
        
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.16.0'
        channel: 'stable'
        
    - name: Get dependencies
      run: flutter pub get
      
    - name: Run tests
      run: flutter test
      
    - name: Build APK
      run: flutter build apk --release
      
    - name: Build App Bundle
      run: flutter build appbundle --release
      
    - name: Upload APK as Artifact
      uses: actions/upload-artifact@v4
      with:
        name: genzspace-apk
        path: build/app/outputs/flutter-apk/app-release.apk
        retention-days: 30
        
    - name: Upload App Bundle as Artifact
      uses: actions/upload-artifact@v4
      with:
        name: genzspace-aab
        path: build/app/outputs/bundle/release/app-release.aab
        retention-days: 30

  build-ios:
    runs-on: macos-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.16.0'
        channel: 'stable'
        
    - name: Get dependencies
      run: flutter pub get
      
    - name: Build iOS
      run: flutter build ios --release --no-codesign
      
    - name: Upload iOS as Artifact
      uses: actions/upload-artifact@v4
      with:
        name: genzspace-ios
        path: build/ios/iphoneos/Runner.app
        retention-days: 30

  notify:
    runs-on: ubuntu-latest
    needs: [build-android, build-ios]
    if: always()
    
    steps:
    - name: Notify Build Status
      run: |
        if [ "${{ needs.build-android.result }}" == "success" ] && [ "${{ needs.build-ios.result }}" == "success" ]; then
          echo "✅ Both Android and iOS builds successful!"
        elif [ "${{ needs.build-android.result }}" == "success" ]; then
          echo "✅ Android build successful!"
        elif [ "${{ needs.build-ios.result }}" == "success" ]; then
          echo "✅ iOS build successful!"
        else
          echo "❌ Build failed"
        fi